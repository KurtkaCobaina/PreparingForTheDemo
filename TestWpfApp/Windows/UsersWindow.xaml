<Window x:Class="TestWpfApp.UsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpfApp"
        mc:Ignorable="d"
        Title="UsersWindow" MinHeight="450" MinWidth="750" Height="550" Width="850" MaxHeight="900" MaxWidth="1500" WindowStartupLocation="CenterScreen" Style="{StaticResource CustomBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ComboBox x:Name="FiltersComboBox"  SelectionChanged="FiltersChanges">
                <ComboBoxItem Content="Все"/>
                <ComboBoxItem Content="По фио"/>
                <ComboBoxItem Content="Логину"/>
            </ComboBox>
            <Button Width="100" Height="20" Content="Выйти" Click="LogOutButton_Click"/>
        </StackPanel>
        <DataGrid Grid.Row="1" AutoGenerateColumns="False" x:Name="UsersDataGrid" Height="350" Width="750" Style="{StaticResource DataGridStyles}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ид" Width="Auto" Binding="{Binding id}"/>
                <DataGridTextColumn  Header="Логин" Width="Auto" Binding="{Binding user_login}"/>
                <DataGridTextColumn  Header="Пароль" Width="Auto" Binding="{Binding user_password}"/>
                <DataGridTextColumn Header="ФИО" Width="Auto" Binding="{Binding fio}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Добавить пользователя" Width="150" Height="50" Margin="0,10,0,0" Click="AddButton_Click"  />
            <Button Content="Редактировать пользователя" Width="150" Height="50" Margin="10,10,0,0" Click="EditButton_Click"/>
            <Button Content="Удалить пользователя" Width="150" Height="50" Margin="10,10,0,0" Click="DeleteButton_Click" />
        </StackPanel>
    </Grid>
</Window>
