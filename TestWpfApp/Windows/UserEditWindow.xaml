<Window x:Class="TestWpfApp.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpfApp"
        mc:Ignorable="d"
        Title="UserEditWindow" MinHeight="370" MinWidth="500" Height="450" Width="800" MaxHeight="740" MaxWidth="1000" WindowStartupLocation="CenterScreen" Style="{StaticResource CustomBackground}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Изменение пользователя" HorizontalAlignment="Center" FontSize="36"/>
          
            <Label Content="Логин"/>
            <TextBox x:Name ="LoginTextBox" Text="{Binding user_login}"/>
            <Label Content="Пароль"/>
            <TextBox x:Name ="PasswordTextBox" Text="{Binding user_password}"/>
            <Label Content="ФИО"/>
            <TextBox x:Name ="FioTextBox" Text="{Binding fio}"/>
         
            <Button Height="30" Margin="0,10,0,0" Content="Сохранить" Click="SaveButton_Click"/>
            <Button Height="30" Margin="0,10,0,0" Content="Назад" Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
